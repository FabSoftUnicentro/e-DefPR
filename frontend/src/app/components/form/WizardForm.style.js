import styled from 'styled-components'

const StyledWizardForm = styled.div`
  .steps-content {
    margin: calc(24px + .5vh) 0;
  }

  .steps-action {
    display: flex;
    align-items: center;
    justify-content: space-between;
  }
`

export {
  StyledWizardForm
}
